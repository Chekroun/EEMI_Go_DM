package main

import (
	"os"

	"devoir10/cmd"
)

/*
Main provides the single access point to stdlib globals, passing them as arguments
to the actual testable entry point.
*/
func main() {
	stdout, stderr := os.Stdout, os.Stderr
	// À vous...
}
